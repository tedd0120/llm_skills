# 会审报告处理与派生规范（阶段二）

本文件供用户收到第一阶段完成通知并下发第二条 prompt 时查阅。

## 执行原则

1. **独立指令驱动**：用户的下一条 prompt 视为全新处理指令。根据其指令对最近一次 `<run-dir>/reports/` 下的多份独立报告进行处理（如交叉对比、提取共识、筛选方案、冲突裁决或生成综合报告）。
2. **产物归档位置**：处理报告生成的派生文档与临时文件，默认存放在对应的 `<run-dir>/` 内，不污染项目源码树。
3. **重新会审触发**：仅当用户明确要求“重新运行”或“更换模型重新评测”时，才开启新一轮第一阶段。
