---
description: 搜索并总结小红书内容（严格遵守分层约束）
---

当用户调用此命令时，请执行以下操作：

1. **确定搜索主题**：解析用户在命令后输入的参数，或询问用户想要探索的主题。

2. **推演关键词**：根据主题推演 2-3 个核心搜索词。

3. **执行抓取**：调用 `xiaohongshu-scraper` Skill 中的 `fetch_xhs.py` 脚本获取原始数据。

4. **🔴 核心要求（必须执行）**：
   - [核心要求] 必须在 `data/xiaohongshu/` 下创建专属结果文件夹
   - [核心要求] 必须直接从 JSON 数据生成增强版汇总报告 `_index.md`
   - [核心要求] 报告必须包含所有分析板块：搜索任务摘要、声量分析、对比矩阵、高频共识、决策建议、价格参考、避雷指南、关键洞察（共8个板块）
   - [核心要求] 禁止跳过任何分析板块或使用简短判语代替详尽分析
   - [核心要求] 必须保持可追溯性，包含帖子标题、作者、日期和关键引用
   - [核心要求] 生成 `_index.md` 后，必须将报告原文直接发送到用户对话框，禁止仅发送文件路径

5. **🟡 标准操作（优先遵守）**：
   - [标准操作] 应当优先使用有头模式（Windows: Edge, Linux: Xvfb）以降低风控风险
   - [标准操作] 应当检查并处理扫码登录需求

6. **🟢 灵活选项（特殊情况）**：
   - [灵活选项] 可在无显示器的 Linux 环境降级使用 `--headless` 模式（风险提示：风控检测概率更高）
   - [灵活选项] 可在数据不足时简化分析板块，标注"样本有限"

请引导用户一步步完成搜索、登录（如果需要扫码）到最终总结产出的全过程。
