---
description: 抓取小红书帖子和评论并在 Agent 场景中进行 AI 总结
---

# 小红书内容抓取 Skill

通过自动化浏览器（Playwright）抓取小红书上的帖子正文和评论区内容。作为 Agent，你需要调用此 Skill 提供的脚本获取原始数据，然后将数据进行 AI 总结并整理输出。

## 🎯 前置依赖配置

请确保用户在 `.env` 中添加了此配置，用于保存登录的持久化 state（Cookie 等）：
```env
XHS_AUTH_STATE=.agent/skills/xiaohongshu-scraper/scripts/xhs_auth.json
```

需要安装脚本依赖：
```bash
pip install -r .agent/skills/xiaohongshu-scraper/scripts/requirements.txt
```

对于 Windows 有头模式，脚本会优先使用系统 Edge (`channel="msedge"`) 以减少风控特征。

### Linux 无屏幕环境配置（Xvfb 虚拟显示器）

在 Linux 服务器（无物理显示器）上运行时，[标准操作] 应当使用 Xvfb 创建虚拟屏幕以启动**有头模式浏览器**，这比 headless 模式更不容易被小红书检测：

```bash
# 1. 安装 Xvfb 和 Chromium 依赖
sudo apt-get update
sudo apt-get install -y xvfb libgbm1 libnss3 libatk-bridge2.0-0 libxkbcommon0 \
    libgtk-3-0 libdrm2 libxcomposite1 libxdamage1 libxrandr2 libasound2

# 2. 安装 Playwright 浏览器
playwright install chromium

# 3. 启动虚拟显示器 (1920x1080 分辨率, 24位色深)
Xvfb :99 -screen 0 1920x1080x24 &

# 4. 设置 DISPLAY 环境变量
export DISPLAY=:99

# 5. 正常运行脚本 (不加 --headless，将以有头模式在虚拟屏幕中运行)
python .agent/skills/xiaohongshu-scraper/scripts/fetch_xhs.py --keywords "搜索词"
```

> **提示**: 可将 `export DISPLAY=:99` 写入 `.bashrc` 或 `.env` 中持久化。如果使用 systemd 管理 agent 服务，在 service 文件的 `[Service]` 段添加 `Environment=DISPLAY=:99`。

[灵活选项] 如无法安装 Xvfb，可降级使用 `--headless` 参数（风险提示：风控检测概率更高）。

---

## 🎯 优先级定义与冲突处理

| 优先级 | 标签 | 含义 | 违反后果 |
|-------|-----|------|---------|
| **P0** | [核心要求] | 绝对必须，违反则任务失败 | 无法生成报告 |
| **P1** | [标准操作] | 强烈建议，违反降低质量 | 可靠性降低 |
| **P2** | [灵活选项] | 可选方案，提供替代路径 | 无 |
| **P3** | [参考信息] | 提示信息，无约束力 | 无 |

### 优先级冲突处理
- P0 > P1 > P2 > P3（优先级降序）
- P1 受阻时可降级到 P2
- P3 可忽略

---

## 🛠️ CLI 基础用法

代理脚本采用命令行调用。你可以通过写入临时文件或直接捕获 stdout 获得 JSON 输出。

```bash
# 基础搜索 (默认上限 20 篇结果)
python .agent/skills/xiaohongshu-scraper/scripts/fetch_xhs.py --keywords "2026年春季穿搭"

# 多词联合搜索并指定输出文件, 指定上限 50 篇（硬上限 100）
python .agent/skills/xiaohongshu-scraper/scripts/fetch_xhs.py --keywords "春装搭配,早春outfit" --max-posts 50 --output "data/tmp_xhs_raw.json"

# 强制开启无头模式 (通常在 Linux 环境下或不需要看到浏览器弹窗时)
python .agent/skills/xiaohongshu-scraper/scripts/fetch_xhs.py --keywords "咖啡拉花教程" --headless
```

### 首次登录交互
小红书**必需登录**才能浏览内容：
1. 首次运行时，脚本会自动访问登录页，并截取二维码保存为 `xhs_qr_login.png`。
2. 脚本会在终端输出 `[!] 请扫码: <绝对路径>` 提示。
3. 用户扫码完成后，脚本会自动保存 state JSON，后续再调用即免登陆。

---

## 🤖 Agent 标准操作流

### ⛔ 强制输出规范 (Non-Negotiable)
在执行搜索和总结任务时，你**必须**遵守以下三条铁律。这是绝对不可违反的红线：
1. **必须建目录**：每次搜索**必须**创建一个专属的按约定格式命名的子文件夹（步骤3）。
2. **必须生成增强版汇总报告**：读取 JSON 数据后，**必须**直接生成包含所有分析板块的 `_index.md` 增强版汇总报告（步骤4）。**禁止**跳过任何分析板块，**禁止**使用”导航链接+一句话简短判语”代替详尽分析。
3. **必须保持可追溯性**：报告中的所有结论**必须**可追溯到具体帖子或评论。**必须**包含帖子标题、作者和日期，**必须**包含关键引用（用户反馈、典型评论）。

当用户请求"搜索/探索小红书某个主题"时，你应当执行以下步骤：

### 0. ⚠️ 检测扫码登录需求（关键！）

**运行脚本后，你必须立即检查命令输出**。如果输出中包含 `[!] 请扫码:` 字样，说明需要用户扫码登录。此时你必须：

1. **从输出中提取 QR 码图片的绝对路径**（例如 `P:\git_repos\llm_skills\xhs_qr_login.png`）
2. **立即用 `view_file` 工具读取该图片文件**，然后将图片展示给用户
3. **明确告知用户**：「请使用小红书 App 扫描此二维码登录，扫码后脚本会自动继续」
4. **等待脚本执行完成**——脚本会在用户扫码后自动保存 Cookie 并继续执行搜索任务

> 如果你不主动把二维码图片发给用户，用户将完全不知道需要扫码，脚本会一直等待直到超时退出！

**判断方法**：使用 `command_status` 检查脚本输出，关注是否包含以下关键词：
- `[!] 请扫码:` → 需要展示 QR 码
- `[*] 登录成功` → 扫码已完成
- `[✗] 扫码超时` → 超时失败，需重新运行

### 1. 扩充搜索词
根据用户的主题（例如“近期关于小米汽车的测评”），你需要推演出 2-3 个延伸的核心搜索词。
- 组合关键词：`"小米汽车测评,SU7提车作业,小米SU7驾驶体验"`

### 2. 执行抓取脚本
调用 `fetch_xhs.py` 获取原始数据。[核心要求] 必须保存到临时 JSON。
[参考信息] ⏱️ 过程可能需要几分钟（为了防反爬，有随机秒数的 sleep）。
**启动脚本后立即按「步骤 0」检查是否需要扫码登录！**

### 3. 创建结果目录（⛔ 强制必须）
抓取完成后，**必须立刻**构建专属的本地输出目录：
格式：`data/xiaohongshu/{中文主旨_yyyyMMdd_hhMMss}/`
例如：`data/xiaohongshu/小米汽车测评_20260226_164000/`

### 4. 直接生成增强版汇总报告

读取刚才的 JSON 结果，直接生成完整的 `_index.md` 增强版汇总报告。

`_index.md` 必须包含以下板块（详见步骤 5 模板）：

#### 基础信息板块（必选）
- 搜索任务摘要

#### 深度分析板块（全部包含）
- 品牌/主题声量分析
- 多维对比矩阵
- 评论区高频共识（Top 5 优点 / Top 5 痛点）
- 场景化决策建议
- 价格/规格参考（如适用）
- 避雷指南
- 关键洞察

**数据来源：**
- 品牌/主题声量：统计所有帖子中的品牌/主题提及
- 对比矩阵：基于主贴内容和评论区对比讨论
- 高频共识：分析所有评论（优先高赞评论）
- 决策建议：提炼”什么情况选哪个”模式
- 价格信息：从评论中提取实际成交价

**质量要求：**
- 所有结论可追溯到具体帖子或评论
- 数据不足时明确标注”样本有限”
- 避免主观判断，以评论区观点为依据

**输出要求：**
- [核心要求] 生成 `_index.md` 后，必须将报告原文直接发送到用户对话框
- 禁止仅发送文件路径而不展示内容
- 报告应当以 Markdown 格式完整呈现

### 5. 增强版汇总报告模板

`_index.md` 必须包含以下板块：

#### 必需板块（所有情况）
1. **搜索任务摘要** - 时间、关键词、数据量
2. **品牌/主题声量分析** - 统计主要品牌/话题的讨论热度
3. **多维对比矩阵** - 多维度对比表
4. **评论区高频共识** - Top 优点 & Top 痛点
5. **场景化决策建议** - 决策树/场景推荐
6. **价格/规格参考** - 从评论中提取（如适用）
7. **避雷指南** - 高频问题汇总
8. **关键洞察** - 跨帖洞察总结

#### 数据不足处理
- 当帖子数 < 15 篇时：简化分析板块，标注”样本有限”
- 当某品牌/主题 < 3 篇时：不强行对比，标注”数据不足”

---

```markdown
# 搜索任务: [主旨]

- **执行时间**: [YYYY-MM-DD HH:MM]
- **使用搜索词**: keyword1, keyword2
- **共抓取帖子**: [N] 篇

---

## 📊 品牌/主题声量分析

### 讨论热度分布
| 品牌/主题 | 讨论占比 | 主要提及内容 |
|-----------|---------|-------------|
| [品牌A] | XX% | ... |
| [品牌B] | XX% | ... |

### 多维对比矩阵
| 维度 | [品牌A] | [品牌B] | [品牌C] |
|-----|---------|---------|---------|
| [维度1] | ... | ... | ... |
| [维度2] | ... | ... | ... |

---

## 💬 评论区高频共识

### 核心优点 (Top 5)
1. **[优点]** - 提及 ~X 次，主要涉及 [品牌/主题]
2. ...

### 核心痛点 (Top 5)
1. **[痛点]** - 提及 ~X 次，主要涉及 [品牌/主题]
2. ...

---

## 🎯 场景化决策建议

### 决策树
```
[ASCII 决策树或场景推荐]
```

### 场景推荐
- **[场景1]**: [建议] - 理由：...
- **[场景2]**: [建议] - 理由：...

---

## 💰 价格/规格参考
[从评论中提取的价格/规格信息]

---

## ⚠️ 避雷指南

### [品牌A/主题A]
- **高频问题**: [问题描述]
- **用户反馈**: [引用评论]

### [品牌B/主题B]
- ...

---

## 🔍 关键洞察

1. **[洞察1]**: [详细说明]
2. **[洞察2]**: [详细说明]
3. **[洞察3]**: [详细说明]
```

---

## ⚠️ 常见限制与故障排除
- **风控封禁**：脚本有等待延迟、加载翻页上限，尽量防止被小红书拉黑。因此**绝不要改动强行取消 script 中的延时**。
- **元素选择器失效**：如果抓出的标题为空，可能是小红书改版。[核心要求] 必须检查 `scripts/selectors.py` 并借助调试功能重新寻找新类名。
- **二维码过期或等待超时**：脚本会异常退出，[核心要求] 必须重新调用重试触发新的一轮扫码。
