---
name: xiaohongshu-scraper
description: 搜索并总结小红书内容（严格遵守分层约束）
license: MIT
metadata:
  author: llm-skills
  version: "2.0"
---

# 小红书内容抓取 Skill

通过自动化浏览器（Playwright）抓取小红书上的帖子正文和评论区内容。作为 Agent，你需要调用此 Skill 提供的脚本获取原始数据，然后将数据进行 AI 总结并整理输出。

---

## 🎯 两阶段执行流程

当用户调用此命令时，请按以下两个阶段执行：

### 阶段一：澄清阶段（必须执行）

此阶段的目标是与用户确认搜索关键词，确保爬虫抓取的内容符合用户意图。**确认前禁止进入执行阶段**。

#### 1. 解析用户输入

- **将用户输入视为整体文本**，忽略内部所有分隔符（逗号、空格、分号等）
- 理解用户建议的主旨，识别核心要素：主题、地域、场景、意图
- **示例**：输入 "广州装修公司, 对比, 避坑" 应理解为 "广州装修公司相关" 这一整体建议

#### 2. 衍生搜索关键词

- **必须衍生恰好 3 个搜索关键词**，不得多于 3 个，不得少于 3 个
- 所有衍生词必须符合用户建议的主旨，保留关键要素（如地域、场景）
- **建议从不同角度衍生**（但不强制）：
  - 直译/直接搜索（最接近原意）
  - 对比/评价（获取他人经验）
  - 避雷/负面（获取风险信息）

#### 3. 展示并征求反馈

使用以下格式向用户展示衍生关键词：

```
我基于你的建议衍生了以下 3 个搜索词：

1. [关键词一]
2. [关键词二]
3. [关键词三]

回复：
• "确认" - 开始爬取
• 其他内容 - 继续调整
```

#### 4. 循环调整

- 根据用户反馈修改关键词
- 重新展示更新后的关键词
- **主动询问**："确认开始爬取吗？"
- 直到用户明确确认（回复"确认"、"开始"、"好的"、"就这样"等）

**澄清阶段完成标志**：用户明确确认关键词，进入阶段二。

---

### 阶段二：执行阶段（确认后进入）

此阶段执行实际的爬虫操作和报告生成。详细步骤见后文"Agent 执行详细步骤"。

---

## 🎯 优先级定义与冲突处理

### 优先级标签体系

| 优先级 | 标签 | 含义 | 违反后果 |
|-------|-----|------|---------|
| **P0** | [核心要求] | 绝对必须，违反则任务失败 | 无法生成报告 |
| **P1** | [标准操作] | 强烈建议，违反降低质量 | 可靠性降低 |
| **P2** | [灵活选项] | 可选方案，提供替代路径 | 无 |
| **P3** | [参考信息] | 提示信息，无约束力 | 无 |

### 优先级冲突处理规则

- P0 > P1 > P2 > P3（优先级降序）
- P0 受阻时：必须停止任务并报告失败
- P1 受阻时：可降级使用 P2 方案，并标注降级原因
- P2/P3 受阻时：可忽略该指令并继续任务

### 核心要求概览

以下为核心要求清单，违反任何一项将导致任务失败：

1. **[核心要求]** 必须完成澄清阶段后才能进入执行阶段
2. **[核心要求]** 必须衍生恰好 3 个关键词，且符合用户主旨
3. **[核心要求]** 必须在 `data/xiaohongshu/` 下创建专属结果文件夹
4. **[核心要求]** 必须直接从 JSON 数据生成增强版汇总报告 `_index.md`
5. **[核心要求]** 报告必须包含 8 个分析板块，禁止跳过或简化
6. **[核心要求]** 报告结论必须可追溯到具体帖子或评论
7. **[核心要求]** 生成报告后，必须将报告原文直接发送到用户对话框

---

## 🎯 前置依赖配置

### 环境变量配置

请确保用户在 `.env` 中添加了此配置，用于保存登录的持久化 state（Cookie 等）：

```env
XHS_AUTH_STATE=.agent/skills/xiaohongshu-scraper/scripts/xhs_auth.json
```

### 依赖安装

```bash
pip install -r .agent/skills/xiaohongshu-scraper/scripts/requirements.txt
```

### Windows 环境

脚本会优先使用系统 Edge (`channel="msedge"`) 以减少风控特征。

### Linux 无屏幕环境配置（Xvfb 虚拟显示器）

在 Linux 服务器（无物理显示器）上运行时，[标准操作] 应当使用 Xvfb 创建虚拟屏幕以启动**有头模式浏览器**，这比 headless 模式更不容易被小红书检测：

```bash
# 1. 安装 Xvfb 和 Chromium 依赖
sudo apt-get update
sudo apt-get install -y xvfb libgbm1 libnss3 libatk-bridge2.0-0 libxkbcommon0 \
    libgtk-3-0 libdrm2 libxcomposite1 libxdamage1 libxrandr2 libasound2

# 2. 安装 Playwright 浏览器
playwright install chromium

# 3. 启动虚拟显示器 (1920x1080 分辨率, 24位色深)
Xvfb :99 -screen 0 1920x1080x24 &

# 4. 设置 DISPLAY 环境变量
export DISPLAY=:99

# 5. 正常运行脚本 (不加 --headless，将以有头模式在虚拟屏幕中运行)
python .agent/skills/xiaohongshu-scraper/scripts/fetch_xhs.py --keywords "搜索词"
```

> **[参考信息]**：可将 `export DISPLAY=:99` 写入 `.bashrc` 或 `.env` 中持久化。如果使用 systemd 管理 agent 服务，在 service 文件的 `[Service]` 段添加 `Environment=DISPLAY=:99`。

> **[灵活选项]**：如无法安装 Xvfb，可降级使用 `--headless` 参数（风险提示：风控检测概率更高）。

---

## 🤖 Agent 执行详细步骤

### 步骤 0：检测扫码登录需求（关键！）

**运行脚本后，你必须立即检查命令输出**。如果输出中包含 `[!] 请扫码:` 字样，说明需要用户扫码登录。

**处理流程：**

1. **从输出中提取 QR 码图片的绝对路径**（例如 `P:\git_repos\llm_skills\xhs_qr_login.png`）
2. **立即用 `Read` 工具读取该图片文件**，然后将图片展示给用户
3. **明确告知用户**：「请使用小红书 App 扫描此二维码登录，扫码后脚本会自动继续」
4. **等待脚本执行完成**——脚本会在用户扫码后自动保存 Cookie 并继续执行搜索任务

> [参考信息] 如果你不主动把二维码图片发给用户，用户将完全不知道需要扫码，脚本会一直等待直到超时退出！

**判断方法**：检查脚本输出中的关键词：
- `[!] 请扫码:` → 需要展示 QR 码
- `[*] 登录成功` → 扫码已完成
- `[✗] 扫码超时` → 超时失败，需重新运行

---

### 步骤 1：执行抓取脚本

调用 `fetch_xhs.py` 获取原始数据。

**命令格式：**

```bash
python .agent/skills/xiaohongshu-scraper/scripts/fetch_xhs.py --keywords "关键词1,关键词2,关键词3"
```

**可选参数：**
- `--max-posts N`：指定帖子上限（默认 20，硬上限 100）
- `--output PATH`：指定 JSON 输出路径
- `--headless`：强制无头模式

**[参考信息]** ⏱️ 过程可能需要几分钟（为了防反爬，脚本内置随机延时）。

---

### 步骤 2：创建结果目录

抓取完成后，**必须立刻**构建专属的本地输出目录。

**目录格式：** `data/xiaohongshu/{中文主旨_yyyyMMdd_hhMMss}/`

**示例：** `data/xiaohongshu/小米汽车测评_20260226_164000/`

---

### 步骤 3：生成增强版汇总报告

读取刚才的 JSON 结果，直接生成完整的 `_index.md` 增强版汇总报告。

#### 报告必需板块（8个）

| 板块 | 说明 |
|-----|------|
| 1. 搜索任务摘要 | 时间、关键词、数据量 |
| 2. 品牌/主题声量分析 | 统计主要品牌/话题的讨论热度 |
| 3. 多维对比矩阵 | 多维度对比表（至少3个维度） |
| 4. 评论区高频共识 | Top 5 优点 & Top 5 痛点 |
| 5. 场景化决策建议 | 决策树/场景推荐 |
| 6. 价格/规格参考 | 从评论中提取（如适用） |
| 7. 避雷指南 | 高频问题汇总 |
| 8. 关键洞察 | 跨帖洞察总结（至少3条） |

#### 数据来源规则

- **品牌/主题声量**：统计所有帖子中的品牌/主题提及
- **对比矩阵**：基于主贴内容和评论区对比讨论
- **高频共识**：分析所有评论（优先高赞评论）
- **决策建议**：提炼"什么情况选哪个"模式
- **价格信息**：从评论中提取实际成交价

#### 质量要求

- 所有结论**必须可追溯到具体帖子或评论**
- **必须包含**：帖子标题、作者、日期、关键引用
- 数据不足时明确标注"样本有限"
- 避免主观判断，以评论区观点为依据

#### 输出要求

- **[核心要求]** 生成 `_index.md` 后，**必须将报告原文直接发送到用户对话框**
- 禁止仅发送文件路径而不展示内容
- 报告应当以 Markdown 格式完整呈现

#### 数据不足处理

- 当帖子数 < 15 篇时：标注"样本有限"，但不跳过任何板块
- 当某品牌/主题 < 3 篇时：标注"数据不足"，不强行对比
- 当某板块不适用时（如教程类主题不适合价格对比），标注"此板块不适用于当前主题"并说明理由

---

## 📄 增强版汇总报告模板

```markdown
# 搜索任务: [主旨]

- **执行时间**: [YYYY-MM-DD HH:MM]
- **使用搜索词**: keyword1, keyword2, keyword3
- **共抓取帖子**: [N] 篇

---

## 📊 品牌/主题声量分析

### 讨论热度分布
| 品牌/主题 | 讨论占比 | 主要提及内容 |
|-----------|---------|-------------|
| [品牌A] | XX% | ... |
| [品牌B] | XX% | ... |

### 多维对比矩阵
| 维度 | [品牌A] | [品牌B] | [品牌C] |
|-----|---------|---------|---------|
| [维度1] | ... | ... | ... |
| [维度2] | ... | ... | ... |

---

## 💬 评论区高频共识

### 核心优点 (Top 5)
1. **[优点]** - 提及 ~X 次，主要涉及 [品牌/主题]
2. ...

### 核心痛点 (Top 5)
1. **[痛点]** - 提及 ~X 次，主要涉及 [品牌/主题]
2. ...

---

## 🎯 场景化决策建议

### 决策树
```
[ASCII 决策树或场景推荐]
```

### 场景推荐
- **[场景1]**: [建议] - 理由：...
- **[场景2]**: [建议] - 理由：...

---

## 💰 价格/规格参考
[从评论中提取的价格/规格信息]

---

## ⚠️ 避雷指南

### [品牌A/主题A]
- **高频问题**: [问题描述]
- **用户反馈**: [引用评论]

### [品牌B/主题B]
- ...

---

## 🔍 关键洞察

1. **[洞察1]**: [详细说明]
2. **[洞察2]**: [详细说明]
3. **[洞察3]**: [详细说明]
```

---

## 🛠️ CLI 基础用法（直接脚本调用）

以下命令供直接调用脚本时参考：

```bash
# 基础搜索 (默认上限 20 篇结果)
python .agent/skills/xiaohongshu-scraper/scripts/fetch_xhs.py --keywords "2026年春季穿搭"

# 多词联合搜索并指定输出文件, 指定上限 50 篇（硬上限 100）
python .agent/skills/xiaohongshu-scraper/scripts/fetch_xhs.py --keywords "春装搭配,早春outfit" --max-posts 50 --output "data/tmp_xhs_raw.json"

# 强制开启无头模式 (通常在 Linux 环境下或不需要看到浏览器弹窗时)
python .agent/skills/xiaohongshu-scraper/scripts/fetch_xhs.py --keywords "咖啡拉花教程" --headless
```

**首次登录交互：**
小红书**必需登录**才能浏览内容：
1. 首次运行时，脚本会自动访问登录页，并截取二维码保存为 `xhs_qr_login.png`
2. 脚本会在终端输出 `[!] 请扫码: <绝对路径>` 提示
3. 用户扫码完成后，脚本会自动保存 state JSON，后续再调用即免登陆

---

## ⚠️ 常见限制与故障排除

| 问题 | 原因 | [核心要求] 处理方式 |
|-----|------|-------------------|
| 风控封禁 | 小红书检测到自动化 | 脚本已内置延时和翻页上限，**绝不要改动强行取消延时** |
| 元素选择器失效 | 小红书页面改版 | **[核心要求]** 必须检查 `scripts/selectors.py` 并重新寻找新类名 |
| 二维码过期或超时 | 登录等待时间过长 | **[核心要求]** 必须重新调用重试触发新的一轮扫码 |
| 抓取标题为空 | 选择器失效或帖子类型特殊 | 检查 `scripts/selectors.py`，确认视频/图文帖的选择器 |

---

## 📝 重要提醒

- **必须**完成澄清阶段后才能进入执行阶段
- **必须**衍生恰好 3 个关键词
- **必须**保持衍生词符合用户主旨
- 禁止在用户确认前调用 fetch_xhs.py

请引导用户一步步完成澄清、确认、爬取到最终总结产出的全过程。
