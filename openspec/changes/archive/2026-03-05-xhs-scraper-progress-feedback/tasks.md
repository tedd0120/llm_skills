## 1. Python 脚本修改

- [x] 1.1 为 `fetch_xhs.py` 中所有进度 `print()` 语句添加 `flush=True` 参数
  - 关键词搜索开始输出（约第 99 行）
  - 找到帖子数输出（约第 195 行）
  - 单篇进度输出（约第 199 行）
  - 完成通知输出（约第 115 行）

## 2. Agent 执行指导更新

- [x] 2.1 在 `skill.md` 步骤 2 中添加后台运行模式说明
  - 修改命令格式，使用 `run_in_background=true`
  - 添加 task_id 保存说明

- [x] 2.2 在 `skill.md` 中添加进度轮询循环示例
  - 轮询间隔建议（约 5 秒）
  - 进度解析和展示逻辑
  - 完成检测和错误处理

- [x] 2.3 添加进度去重逻辑（新增）
  - 保存上一次显示的进度状态
  - 只在进度变化时更新显示
  - 使用进度条格式：`[n/6] ██████░░░░░░░░░ n%`

- [x] 2.4 添加静默轮询规则（修复）
  - 在 skill.md 中添加明确的禁止规则
  - 禁止输出"等待中..."、"轮询中..."等任何等待消息
  - 只在进度实际变化时才输出
  - 确保 6 篇帖子只有 6 条输出

## 3. 更新 skill.md 进度展示格式

- [x] 3.1 更新轮询循环示例代码
  - 添加去重逻辑的伪代码
  - 添加进度条生成函数

## 3. 测试验证

- [x] 3.1 验证 Python 脚本输出实时性
  - 运行脚本确认输出不被缓冲

- [x] 3.2 验证后台运行 + 轮询模式
  - 测试进度能否实时获取和展示
  - 测试错误处理（非零退出码）
