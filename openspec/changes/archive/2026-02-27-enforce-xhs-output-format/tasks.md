# 实施任务: 强制小红书抓取输出格式

## 1. 更新 `SKILL.md` 指令内容

- [x] 1.1 在文件开头/操作流最前面添加「⛔ 强制输出规范 (Non-Negotiable)」段落。
- [x] 1.2 将原本的 3 条核心要求（必须建文件夹、必须每贴生成MD、必须最后汇总）列入绝对禁止违反的红线。
- [x] 1.3 更新步骤 3（创建结果目录）的措辞，从建议改为强制必须。
- [x] 1.4 更新步骤 4（数据总结写入）的措辞，增加绝对禁止跳过、禁止合并生成单文件的警告。
- [x] 1.5 更新步骤 5（生成索引 `_index.md`），明确要求必须在单篇 MD 全部完成后生成，且必须包含对单篇 MD 的链接引用。

## 2. 验证和修复语法

- [x] 2.1 检查修改后的 markdown 格式和链接是否在 `SKILL.md` 中正确渲染。
- [x] 2.2 确认警示标志（如 ⛔ 或 ⚠️）能够在常用编辑器/查看器中正常显示。
