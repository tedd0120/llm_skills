## 1. 新增统计输出函数

- [x] 1.1 在 `fetch_attendance.py` 中新增 `print_statistics()` 函数
- [x] 1.2 实现已工作天数计算（日期 < today AND isrest = 0）
- [x] 1.3 实现已累计工时计算（日期 < today 的工时总和）
- [x] 1.4 实现当前平均工时计算（已累计工时 / 已工作天数）
- [x] 1.5 实现剩余工作日计算（本月范围内，日期 >= today AND isrest = 0）
- [x] 1.6 实现基础统计表格输出格式
- [x] 1.7 实现达标预测计算逻辑（目标 10/10.5/11 小时）
- [x] 1.8 实现达标预测表格输出格式
- [x] 1.9 处理"本月已结束"特殊情况（剩余工作日 = 0）

## 2. 修改现有函数

- [x] 2.1 修改 `parsed_att_date()` 函数，强制保存 CSV 到默认路径
- [x] 2.2 在 `parsed_att_date()` 中调用 `print_statistics()` 替换原 print 语句
- [x] 2.3 修改 `main()` 函数，默认 `output_path` 改为 `data/attendance_{YYYYMM}.csv`
- [x] 2.4 确保函数返回值保持不变（向后兼容）

## 3. 更新文档

- [x] 3.1 更新 `SKILL.md` 中的输出示例以反映新格式
- [x] 3.2 添加新的输出格式说明到 SKILL.md

## 4. 测试验证

- [x] 4.1 测试正常月份的统计输出
- [x] 4.2 测试无考勤数据的情况
- [x] 4.3 测试本月已结束的情况
- [x] 4.4 验证 CSV 文件正确保存
- [x] 4.5 验证代码调用向后兼容性
