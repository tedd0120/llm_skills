# 任务清单

## 核心实现
- [x] 1. 修改 `fetch_attendance.py`
  - 为 `parsed_att_date()` 添加 `output_path` 可选参数
  - CLI 增加 `--output` / `-o` 参数
  - 当提供路径时保存 DataFrame 为 CSV

- [x] 2. 更新 `SKILL.md` 文档
  - 添加 CSV 导出使用示例
  - 更新参数说明表

## 验证
- [x] 3. 运行脚本验证 CSV 导出功能
