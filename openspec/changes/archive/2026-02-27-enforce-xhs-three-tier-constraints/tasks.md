## 1. 修改 SKILL.md

- [x] 1.1 读取 `.agent/skills/xiaohongshu-scraper/SKILL.md` 完整内容
- [x] 1.2 在文档开头（第 9 行"🛠️ CLI 基础用法"之前）插入"优先级定义与冲突处理"章节：
  ```
  ## 🎯 优先级定义与冲突处理

  | 优先级 | 标签 | 含义 | 违反后果 |
  |-------|-----|------|---------|
  | **P0** | [核心要求] | 绝对必须，违反则任务失败 | 无法生成报告 |
  | **P1** | [标准操作] | 强烈建议，违反降低质量 | 可靠性降低 |
  | **P2** | [灵活选项] | 可选方案，提供替代路径 | 无 |
  | **P3** | [参考信息] | 提示信息，无约束力 | 无 |

  ### 优先级冲突处理
  - P0 > P1 > P2 > P3（优先级降序）
  - P1 受阻时可降级到 P2
  - P3 可忽略
  ```
- [x] 1.3 替换第 25 行："推荐使用 Xvfb" → "[标准操作] 应当使用 Xvfb"
- [x] 1.4 替换第 48 行："如果不想安装 Xvfb，也可以直接使用" → "[灵活选项] 如无法安装 Xvfb，可降级使用"
- [x] 1.5 替换第 106 行："建议保存到临时 JSON" → "[核心要求] 必须保存到临时 JSON"
- [x] 1.6 替换第 107 行："注意：过程可能需要..." → "[参考信息] ⏱️ 过程可能需要..."
- [x] 1.7 替换第 252 行："检查 scripts/selectors.py" → "[核心要求] 必须检查 scripts/selectors.py"
- [x] 1.8 替换第 253 行："需让用户重新调用重试" → "[核心要求] 必须重新调用重试"
- [x] 1.9 检查文档中所有"建议"、"推荐"、"可以"等模糊词汇，确保全部替换为带标签的明确表述
- [x] 1.10 验证修改后的 SKILL.md，确认：
  - 优先级定义章节已添加
  - 所有 P0 步骤使用"必须/禁止/绝对"
  - 所有 P1 步骤使用"应当/要求"
  - 所有 P2 步骤使用"可/可以/可选"
  - 所有 P3 步骤使用"注意/提示"

## 2. 同步修改 workflow.md

- [x] 2.1 读取 `.agent/workflows/xiaohongshu-scraper.md` 完整内容
- [x] 2.2 在步骤 4（"⚠️ 强制输出流程"）的每个子步骤前添加 `[核心要求]` 标签：
  - "在 `data/xiaohongshu/` 下创建专属结果文件夹" → "[核心要求] 必须在 `data/xiaohongshu/` 下创建专属结果文件夹"
  - "必须直接从 JSON 数据生成增强版汇总报告" → "[核心要求] 必须直接从 JSON 数据生成增强版汇总报告"
  - "报告必须包含所有分析板块" → "[核心要求] 报告必须包含所有分析板块：声量分析、对比矩阵、高频共识、决策建议、价格参考、避雷指南、关键洞察"
  - "绝对禁止跳过任何分析板块" → "[核心要求] 禁止跳过任何分析板块或使用简短判语代替详尽分析"
  - "必须保持可追溯性" → "[核心要求] 必须保持可追溯性，包含原小红书链接和关键引用"
- [x] 2.3 添加新步骤"5. 🟡 标准操作（优先遵守）"：
  - "[标准操作] 应当优先使用有头模式（Windows: Edge, Linux: Xvfb）以降低风控风险"
  - "[标准操作] 应当检查并处理扫码登录需求"
- [x] 2.4 添加新步骤"6. 🟢 灵活选项（特殊情况）"：
  - "[灵活选项] 可在无显示器的 Linux 环境降级使用 `--headless` 模式（风险提示：风控检测概率更高）"
  - "[灵活选项] 可在数据不足时简化分析板块，标注"样本有限""
- [x] 2.5 更新 workflow description："严格遵守输出格式规范" → "严格遵守分层约束"
- [x] 2.6 验证修改后的 workflow.md，确认与 SKILL.md 约束体系一致

## 3. 最终验证

- [x] 3.1 对比 SKILL.md 和 workflow.md，确认标签使用一致
- [x] 3.2 搜索两个文件中是否仍有"建议"、"推荐"等遗留模糊词汇
- [x] 3.3 确认优先级定义章节在 SKILL.md 开头位置正确
- [x] 3.4 确认所有 P0 核心要求都有明确的"必须"关键词
- [x] 3.5 确认所有冲突处理规则已明确说明
