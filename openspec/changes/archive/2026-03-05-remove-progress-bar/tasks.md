## 1. 移除进度轮询循环逻辑

- [x] 1.1 删除步骤 2.2 "进度轮询循环（带去重）" 章节及其所有内容
- [x] 1.2 删除 `renderProgressBar` 进度条渲染函数伪代码
- [x] 1.3 删除进度条展示效果示例（`████░░░░░░░░░░░ 17%` 等）
- [x] 1.4 删除 `extractLatestProgress` 和 `extractCurrentKeyword` 解析函数
- [x] 1.5 删除静默轮询规则相关说明

## 2. 移除核心要求约束

- [x] 2.1 删除 `[核心要求] 必须在轮询循环中主动向用户展示进度` 约束
- [x] 2.2 删除 `[核心要求] 必须在轮询循环中主动向用户展示进度，不能等任务完成后才一次性展示` 约束

## 3. 重构步骤 2 结构

- [x] 3.1 将原步骤 2.3 重命名为步骤 2.2 "等待完成并处理结果"
- [x] 3.2 更新步骤 2.2 内容，使用 `TaskOutput` with `block=true` 等待任务完成
- [x] 3.3 添加完成时的简短完成消息说明

## 4. 验证

- [x] 4.1 确认步骤 2.1 "启动后台抓取任务" 保持不变
- [x] 4.2 确认脚本端输出格式说明保留
- [x] 4.3 确认 `fetch_xhs.py` 脚本无需修改
