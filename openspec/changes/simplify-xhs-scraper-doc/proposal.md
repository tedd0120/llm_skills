## 为什么

`xiaohongshu-scraper` 的 SKILL.md 存在**演进膨胀**问题，当前 801 行文档中约 72% 内容为冗余或越界内容。作为编排层 skill，它包含了大量属于子 skill 的详细规范（报告模板、引用格式、抓取命令等），违反了单一职责原则，增加了维护成本和理解负担。

## 变更内容

- **删除跨文件重复内容**：报告模板、引用格式规范已在 `xiaohongshu-summarize` 中定义，无需在 scraper 中重复
- **删除章节重复**：「核心要求清单」与「重要提醒」章节内容重复，合并为一处
- **删除 CLI 基础用法章节**：详细命令示例已在 `xiaohongshu-fetch` 中定义
- **精简执行步骤**：将具体命令替换为「参照子 skill 文档」的引用
- **精简核心要求清单**：仅保留编排层相关的约束，删除属于子 skill 的要求

## 功能 (Capabilities)

### 新增功能

无新增功能。

### 修改功能

- `xiaohongshu-scraper`: 明确编排层职责边界，子 skill 仅用一句话概括并引用其文档，核心要求清单聚焦编排层约束

## 影响

- **修改文件**：
  - `.claude/skills/xiaohongshu-scraper/SKILL.md`：从 801 行精简至约 220 行

- **不影响行为**：此次变更仅调整文档结构，不改变任何运行时行为
- **向后兼容**：完全兼容
